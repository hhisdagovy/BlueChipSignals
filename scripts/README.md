# scripts/

One-off Node scripts for Firestore export, Auth import, and entitlement backfill were removed after cutover. Use **Supabase SQL Editor** and **`docs/`** instead (see `docs/supabase-auth-cutover.md`, `docs/sql/`).
